{
  "name": "sounds",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start-dev": "lerna run start --scope=@sounds/client --parallel",
    "start-server": "lerna run start --scope=@sounds/server --parallel",
    "start-server-ts": "lerna run start-ts --scope=@sounds/server --parallel"
  },
  "devDependencies": {
    "lerna": "^3.13.0"
  },
  "prettier": {
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  }
}